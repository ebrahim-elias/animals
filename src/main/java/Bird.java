/**
 * sub class of a animal class witch represent the bird animal
 */
public class Bird extends Animal {
    /**
     * constructor with the same fields in the super class animal
     * @param type the type
     * @param name the name
     * @param yearOfBirth the year of birth
     */
    public Bird(String type, String name, int yearOfBirth) {
        super(type, name, yearOfBirth);
    }
}
